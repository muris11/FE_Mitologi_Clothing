# Approve build scripts for packages that need native compilation
# These are trusted packages from the Next.js ecosystem

# Image optimization library (required for Next.js Image component)
sharp=true

# JavaScript bundler (used by Next.js internally)
esbuild=true

# Native module compilation
@tailwindcss/postcss=true
tailwindcss=true
